# BLACKGIFT AI environment example
# DO NOT commit real secrets. Use your provider's secret store or environment settings.

# OpenAI API key (revoke any key you've posted publicly)
OPENAI_API_KEY=

# Session secret for express-session
SESSION_SECRET=change-me-for-prod

# Redis connection (production Redis URL)
REDIS_URL=redis://localhost:6379

# Firebase Admin: set GOOGLE_APPLICATION_CREDENTIALS to the service account JSON file path
# e.g. GOOGLE_APPLICATION_CREDENTIALS=/run/secrets/firebase_service_account.json
# Or set admin credentials by other secure means in your environment.
GOOGLE_APPLICATION_CREDENTIALS=

# Max tokens to keep from conversation history (adjust to control prompt size/cost)
HISTORY_MAX_TOKENS=3000

# Docker/CI variables (for GitHub Actions example)
DOCKERHUB_REPO=yourdockerhubusername/blackgift-ai
DOCKERHUB_TOKEN=
DOCKERHUB_USERNAME=

# App port
PORT=3000